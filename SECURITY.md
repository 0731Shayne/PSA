# 安全说明

## 报告问题

安全问题不应公开写入 Issue。交付后请通过企业指定的安全联系渠道报告，并附上受影响版本、复现条件和潜在影响。企业应在交付清单中补充实际联系人和响应时限。

## 部署要求

- 生产环境必须使用至少 32 个字符的随机 `SECRET_KEY` 和 PostgreSQL。
- `.env`、数据库备份、模型 API Key、教师邀请码和初始密码不得提交到 Git。
- 必须通过 HTTPS 对外提供服务，并限制数据库和管理端口的网络访问。
- `ALLOWED_ORIGINS` 只填写实际可信前端来源；同域部署可留空。
- 初始教师账号首次登录会被系统强制进入改密页；改密会撤销其他设备上的旧会话。企业仍负责账号停用和人员离职流程。
- 用户可以在“账号与安全”中修改密码或让其他已登录设备立即退出；生产环境不得通过共享初始账号代替实名账号管理。
- 备份应加密、限制访问并定期执行恢复演练。
- `RAGFLOW_API_KEY` 只能由后端读取，不得发给浏览器、写入日志或提交到 Git；RAGFlow 管理界面不应直接暴露到公网。
- RAGFlow 应启用 HTTPS 或部署在受控内网，并限制只有 PSA 后端和运维人员可以访问。
- 课程资料上传前应确认版权、个人信息和保密等级；删除资料时要同时验证 RAGFlow 文档、切片和向量索引已删除。
- 检索片段按不可信输入处理。系统提示已要求模型忽略资料中的指令，但部署方仍应使用经过审计的课程资料，并用提示注入样本做上线测试。
- `local` 模式会把提取后的全文、切片和向量写入 PSA 数据库，只适合作品演示或小规模受控资料；备份、删除和访问控制必须覆盖这些字段。

## 数据保留与删除边界

- 用户可删除自己的答疑会话、实验记录；教师可删除自己的教学包。
- 任务撤回、归档、成员移出不会删除既有作答、提示审计和完成记录，以保留教学证据链。
- 任务采用软截止：截止后仍可提交，但记录会标记为迟交，不能伪装成按时完成。
- 账号、班级证据和备份的最终保存周期、导出与彻底删除流程由部署企业在上线前写入隐私与数据保留制度；数据库级删除或匿名化应经过授权并保留审计记录。

依赖漏洞、镜像扫描、日志审计、WAF、速率限制和隐私合规属于部署环境的持续安全工作，不应以一次代码交付代替。
